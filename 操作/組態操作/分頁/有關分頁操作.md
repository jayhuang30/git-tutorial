# 將 tag 指令的(區域)分頁器關閉

將 tag 指令的分頁器關閉，避免依賴終端機資料庫解析：

```Bash
git config --global pager.tag false
```

# 將 tag 指令的(全域)分頁器關閉

將 tag 指令的(全域)分頁器關閉

```Bash
git config --global core.pager ""
```

# References
[git tag有關的指令 | Gems回覆](https://share.gemini.google/HuCBCfki3feY)
